[package]
name = "rust-2048"
version = "0.1.0"
edition = "2021"

[dependencies]
egui = "0.29"
eframe = { version = "0.29", default-features = false, features = [
    "glow",  # cross-platform GPU backend via glow
] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs-next = "2"
